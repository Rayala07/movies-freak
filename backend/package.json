{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_OPTIONS=--dns-result-order=ipv4first npx nodemon server.js",
    "build": "cd ../frontend && npm install --include=dev && npm run build && cp -r dist ../backend/client",
    "build:win": "cd ../frontend && npm install && npm run build && xcopy /E /I /Y dist ..\\backend\\client",
    "start": "cross-env NODE_OPTIONS=--dns-result-order=ipv4first node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@langchain/core": "^1.1.47",
    "@langchain/google": "^0.1.12",
    "@langchain/google-genai": "^2.1.31",
    "bcrypt": "^6.0.0",
    "cloudinary": "^2.9.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "cross-env": "^10.1.0",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-mongo-sanitize": "^2.2.0",
    "express-rate-limit": "^8.3.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.2.4",
    "multer": "^2.1.1",
    "nanoid": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
